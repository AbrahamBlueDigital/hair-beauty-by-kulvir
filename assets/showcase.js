const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const dateInput=document.querySelector('input[name="date"]');const localToday=new Date(Date.now()-new Date().getTimezoneOffset()*60000).toISOString().split('T')[0];dateInput.min=localToday;
document.getElementById('appointmentForm').addEventListener('submit',event=>{event.preventDefault();const data=new FormData(event.currentTarget);const message=['Hello Kulvir, I would like to request an appointment.','',`Name: ${data.get('name')}`,`Phone: ${data.get('phone')}`,`Service: ${data.get('service')}`,`Preferred date: ${data.get('date')}`,`Preferred time: ${data.get('time')}`,`Notes: ${data.get('notes')||'None'}`,'','Please confirm whether this time is available.'].join('\n');document.getElementById('formStatus').textContent='Opening WhatsApp so you can send your request…';window.open(`https://wa.me/14315888019?text=${encodeURIComponent(message)}`,'_blank','noopener')});
